# WebAutomationTool API Backend

FastAPI backend for authentication and subscription management.

## Quick Start

```bash
poetry install
cp .env.example .env
poetry run uvicorn main:app --reload
```

Docs: http://localhost:8000/docs
